apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "molock.fullname" . }}-config
  labels:
    {{- include "molock.labels" . | nindent 4 }}
data:
  molock-config.yaml: |
{{ .Values.config | toYaml | nindent 4 }}
